There is technically no CSS in this test page as it uses the in-built CSS of the browser. CSS can be added as a file in here should you wish to test. It is assumed if you are doing this you understand the basic context of how to do this.